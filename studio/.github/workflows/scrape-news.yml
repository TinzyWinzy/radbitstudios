name: Scrape News

on:
  schedule:
    - cron: "0 */2 * * *"
  workflow_dispatch:
    inputs:
      force:
        description: "Force scrape (skip cache)"
        required: false
        default: "false"

jobs:
  scrape-news:
    name: Scrape News Feeds
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Trigger News Scraper
        run: |
          curl -s -X POST "${{ secrets.APP_URL }}/api/scraper/news?force=${{ github.event.inputs.force == 'true' && 'true' || 'false' }}" \
            -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}" \
            -w "\nHTTP Status: %{http_code}\n"

  alert-on-failure:
    name: Alert on Failure
    needs: scrape-news
    if: failure()
    runs-on: ubuntu-latest
    steps:
      - name: Send Failure Alert
        run: |
          curl -X POST "${{ secrets.SENTRY_DSN }}/envelope/" \
            -H "Content-Type: application/json" \
            -d '{
              "event_id": "'"$(uuidgen)"'",
              "level": "error",
              "exception": {
                "values": [{
                  "type": "ScraperFailed",
                  "value": "News scraper failed at $(date -u)"
                }]
              },
              "tags": {
                "environment": "production",
                "scraper": "news"
              }
            }'
